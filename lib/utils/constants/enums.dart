// List of Enums

// They cannot be created inside a class

enum TextSizes { extraSmall, small, medium, large, extraLarge }

enum OrderStatus { processing, shipped, delivered , cancelled, returned }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm,
}
